---
// Contact.astro
---

<section id="contact" class="py-20 bg-gradient-to-br from-primary-50 to-accent-50">
  <div class="container mx-auto px-6">
    <div class="max-w-4xl mx-auto text-center mb-16" data-aos="fade-up">
      <h2 class="text-3xl md:text-4xl font-lora font-bold text-primary-700 mb-6">
        Prendre contact
      </h2>
      <div class="w-20 h-1 bg-accent-500 mx-auto mb-8"></div>
      <p class="text-lg text-text-light max-w-2xl mx-auto">
        Vous souhaitez réserver un créneau ou avoir plus d'informations ?
        N'hésitez pas à me contacter.
      </p>
    </div>

    <div class="grid lg:grid-cols-2 gap-12">

      <!-- Informations de contact -->
      <div data-aos="fade-right">
        <div class="bg-white rounded-2xl p-8 shadow-lg h-full">
          <h3 class="text-2xl font-lora font-bold text-primary-700 mb-6">
            Informations pratiques
          </h3>

          <!-- Téléphone -->
          <div class="flex items-center mb-8 group">
            <div class="w-12 h-12 bg-gradient-to-br from-primary-500 to-accent-500 rounded-full flex items-center justify-center mr-4 group-hover:scale-110 transition-transform">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
              </svg>
            </div>
            <div>
              <p class="font-semibold text-primary-700">Téléphone</p>
              <a href="tel:+33652251720" class="text-text-light hover:text-primary-600 transition-colors">
                06 52 25 17 20
              </a>
            </div>
          </div>

          <!-- Email -->
          <div class="flex items-center mb-8 group">
            <div class="w-12 h-12 bg-gradient-to-br from-accent-500 to-secondary-500 rounded-full flex items-center justify-center mr-4 group-hover:scale-110 transition-transform">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
              </svg>
            </div>
            <div>
              <p class="font-semibold text-primary-700">Email</p>
              <a href="mailto:info@hypnoclo.fr" class="text-text-light hover:text-primary-600 transition-colors">
                info@hypnoclo.fr
              </a>
            </div>
          </div>

          <!-- Localisation -->
          <div class="flex items-center mb-8 group">
            <div class="w-12 h-12 bg-gradient-to-br from-secondary-500 to-primary-500 rounded-full flex items-center justify-center mr-4 group-hover:scale-110 transition-transform">
              <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
              </svg>
            </div>
            <div>
              <p class="font-semibold text-primary-700">Localisation</p>
              <a href="#carte" class="text-text-light hover:text-primary-600 transition-colors cursor-pointer font-medium">
                Douarnenez, Finistère
              </a>
              <p class="text-text-light text-sm mt-1">
                Consultations également possibles à domicile
              </p>
            </div>
          </div>

          <!-- Horaires -->
          <div class="bg-primary-50 p-7 rounded-xl border-l-4 border-primary-500">
            <h4 class="font-lora font-bold text-primary-700 mb-4">Horaires de consultation</h4>
            <div class="space-y-2 text-text-light">
              <div class="flex justify-between">
                <span>Lundi - Jeudi</span>
                <span>17h00 - 19h00</span>
              </div>
              <div class="flex justify-between">
                <span>Samedi</span>
                <span>9h00 - 12h00</span>
              </div>
              <div class="flex justify-between">
                <span>Dimanche</span>
                <span>Fermé</span>
              </div>
            </div>
          </div>

          <!-- Tarif -->
          <div class="mt-7 text-center p-5 bg-gradient-to-r from-primary-100 to-accent-100 rounded-xl">
            <p class="text-primary-700 font-semibold">
              Consultation : <span class="text-xl font-bold">60€</span>
            </p>
            <p class="text-primary-700 font-semibold mt-1">
              Consultation à domicile : <span class="text-xl font-bold">80€</span>
            </p>
            <p class="text-text-light text-sm mt-1">
              Durée : 1h à 1h30
            </p>
          </div>
        </div>
      </div>

      <!-- Formulaire de contact -->
      <div data-aos="fade-left" data-aos-delay="200">
        <div class="bg-white rounded-2xl p-8 shadow-lg h-full">
          <h3 class="text-2xl font-lora font-bold text-primary-700 mb-3">
            Demande de renseignements
          </h3>
          <p class="text-text-light mb-6">
            Je prends toujours le temps de lire chaque message avec attention et je vous recontacterai personnellement dans les plus brefs délais.
          </p>

          <form id="contact-form" class="space-y-4" action="https://formsubmit.co/info@hypnoclo.fr" method="POST">
            <!-- Champs cachés pour FormSubmit -->
            <input type="hidden" name="_captcha" value="false">
            <input type="hidden" name="_next" value="">
            <input type="hidden" name="_subject" value="Nouvelle demande de contact - HypnoClo">
            <input type="hidden" name="_template" value="box">
            <input type="hidden" name="_autoresponse" value="Bonjour, merci pour votre message ! J'ai bien reçu votre demande et je vous répondrai personnellement dans les plus brefs délais. À très vite, Clotilde.">

            <div>
              <label for="name" class="block text-primary-700 font-semibold mb-2">
                Nom complet *
              </label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-2.5 border border-primary-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent transition-colors"
                placeholder="Votre nom et prénom"
              />
            </div>

            <div>
              <label for="email" class="block text-primary-700 font-semibold mb-2">
                Email *
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-2.5 border border-primary-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent transition-colors"
                placeholder="votre@email.com"
              />
            </div>

            <div>
              <label for="phone" class="block text-primary-700 font-semibold mb-2">
                Téléphone
              </label>
              <input
                type="tel"
                id="phone"
                name="phone"
                class="w-full px-4 py-2.5 border border-primary-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent transition-colors"
                placeholder="06 52 25 17 20"
              />
            </div>

            <div>
              <label for="subject" class="block text-primary-700 font-semibold mb-2">
                Motif de consultation
              </label>
              <select
                id="subject"
                name="subject"
                class="w-full px-4 py-2.5 border border-primary-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent transition-colors"
              >
                <option value="">Choisir un motif</option>
                <option value="tabac">Arrêt du tabac</option>
                <option value="douleurs">Diminution des douleurs</option>
                <option value="deuil">Accompagnement du deuil</option>
                <option value="sommeil">Sommeil apaisé</option>
                <option value="trauma">Traumas</option>
                <option value="poids">Gestion du poids</option>
                <option value="anxiete-phobies">Anxiété et phobies</option>
                <option value="autre">Autre</option>
              </select>
            </div>

            <div>
              <label for="message" class="block text-primary-700 font-semibold mb-2">
                Message *
              </label>
              <textarea
                id="message"
                name="message"
                rows="3"
                required
                class="w-full px-4 py-2.5 border border-primary-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent transition-colors resize-none"
                placeholder="Décrivez brièvement votre situation et vos attentes..."
              ></textarea>
            </div>

            <div class="text-center">
              <button
                type="submit"
                class="w-full bg-primary-500 text-white px-8 py-3.5 rounded-full hover:bg-primary-600 transition-all duration-300 font-semibold shadow-lg hover:shadow-xl transform hover:-translate-y-1"
              >
                Envoyer ma demande
              </button>
            </div>

            <p class="text-xs text-text-light text-center">
              * Champs obligatoires. Vos données sont traitées de manière confidentielle,
              conformément à notre <a href="/politique-confidentialite" class="text-primary-600 hover:underline">politique de confidentialité</a>.
            </p>
          </form>
        </div>
      </div>

    </div>

    <!-- Carte Google Maps -->
    <div id="carte" class="mt-16" data-aos="fade-up">
      <div class="bg-white rounded-2xl p-8 shadow-lg">
        <h3 class="text-2xl font-lora font-bold text-primary-700 mb-6 text-center">
          Localisation
        </h3>
        <div class="w-full h-96 rounded-xl overflow-hidden">
          <iframe
            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d588.1075627818263!2d-4.336096147690553!3d48.09036110143525!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x4816d0c658586187%3A0xdb161bda0c0270f6!2s65%20Rue%20ar%20V%C3%A9ret%2C%2029100%20Douarnenez!5e0!3m2!1sfr!2sfr!4v1752846697871!5m2!1sfr!2sfr"
            width="100%"
            height="100%"
            style="border:0;"
            allowfullscreen=""
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
            title="Localisation de Clotilde à Douarnenez">
          </iframe>
        </div>
        <div class="mt-4 text-center">
          <p class="text-text-light">
            <strong>Douarnenez, Finistère</strong><br>
            Consultations sur rendez-vous uniquement
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Script amélioré pour détecter le succès FormSubmit
  window.addEventListener('load', function() {
    const form = document.getElementById('contact-form');

    // Vérifier les paramètres URL pour détecter le succès
    const urlParams = new URLSearchParams(window.location.search);
    const isSuccess = urlParams.get('form') === 'success' ||
                     window.location.search.includes('form=success') ||
                     document.referrer.includes('formsubmit.co');

    if (isSuccess) {
      // Attendre un peu pour que la page soit complètement chargée
      setTimeout(() => {
        showSuccessMessage();

        // Nettoyer l'URL
        const url = new URL(window.location);
        url.searchParams.delete('form');
        window.history.replaceState({}, document.title, url);
      }, 500);
    }

    form?.addEventListener('submit', function(e) {
      // Calculer _next dynamiquement (fonctionne en dev comme en prod,
      // quel que soit le domaine final)
      const nextField = form.querySelector('input[name="_next"]');
      if (nextField) {
        nextField.value = `${window.location.origin}${window.location.pathname}?form=success#contact`;
      }

      const submitButton = form.querySelector('button[type="submit"]');

      // Changer l'apparence du bouton
      submitButton.textContent = 'Envoi en cours...';
      submitButton.disabled = true;
      submitButton.classList.add('opacity-75');

      // FormSubmit va gérer la redirection
    });

    function showSuccessMessage() {
      const formContainer = document.querySelector('[data-aos="fade-left"]');

      // Créer le message de succès avec animation
      const successMessage = document.createElement('div');
      successMessage.className = 'fixed top-4 right-4 bg-green-100 border border-green-400 text-green-700 px-6 py-4 rounded-lg shadow-lg z-50 max-w-sm transform translate-x-full transition-transform duration-500';
      successMessage.innerHTML = `
        <div class="flex items-start">
          <svg class="w-6 h-6 text-green-600 mr-3 mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
          </svg>
          <div>
            <p class="font-semibold">Message envoyé !</p>
            <p class="text-sm mt-1">Je vous répondrai rapidement.</p>
          </div>
          <button onclick="this.parentElement.parentElement.remove()" class="ml-2 text-green-600 hover:text-green-800">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"></path>
            </svg>
          </button>
        </div>
      `;

      // Ajouter au body
      document.body.appendChild(successMessage);

      // Animer l'entrée
      setTimeout(() => {
        successMessage.classList.remove('translate-x-full');
        successMessage.classList.add('translate-x-0');
      }, 100);

      // Réinitialiser le formulaire
      form.reset();

      // Scroll vers le formulaire
      document.querySelector('#contact').scrollIntoView({
        behavior: 'smooth',
        block: 'center'
      });

      // Supprimer automatiquement après 6 secondes
      setTimeout(() => {
        successMessage.classList.add('translate-x-full');
        setTimeout(() => {
          if (successMessage.parentElement) {
            successMessage.remove();
          }
        }, 500);
      }, 6000);
    }
  });
</script>
